<resources>
    <!-- Base application theme. -->
    <style name="Theme.UnipiaudioStories" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:statusBarColor">@color/colorPrimaryDark</item>
        <item name="android:windowBackground">@color/colorBackground</item>
        <item name="android:textColor">@color/colorText</item>
        <!-- Use pixel art font -->
        <item name="android:fontFamily">monospace</item>
        <!-- Override Material Components styles for pixel art look -->
        <item name="materialButtonStyle">@style/PixelButton</item>
        <item name="cardViewStyle">@style/PixelCardView</item>
        <item name="textInputStyle">@style/PixelTextInputLayout</item>
    </style>

    <style name="PixelButton" parent="Widget.MaterialComponents.Button">
        <item name="android:background">@drawable/pixel_button_background</item>
        <item name="android:textColor">@color/pixelDark</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:fontFamily">monospace</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="PixelCardView" parent="Widget.MaterialComponents.CardView">
        <item name="cardCornerRadius">0dp</item>
        <item name="cardElevation">0dp</item>
        <item name="contentPadding">2dp</item>
        <item name="cardBackgroundColor">@color/pixelBackground</item>
        <item name="strokeWidth">3dp</item>
        <item name="strokeColor">@color/pixelDark</item>
    </style>

    <style name="PixelTextInputLayout" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="boxCornerRadiusBottomEnd">0dp</item>
        <item name="boxCornerRadiusBottomStart">0dp</item>
        <item name="boxCornerRadiusTopEnd">0dp</item>
        <item name="boxCornerRadiusTopStart">0dp</item>
        <item name="boxStrokeWidth">3dp</item>
        <item name="boxStrokeColor">@color/pixelDark</item>
    </style>

    <style name="Theme.UnipiaudioStories.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.UnipiaudioStories.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.UnipiaudioStories.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>